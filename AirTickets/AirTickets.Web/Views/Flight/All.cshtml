<div class="panel">
    @using (Ajax.BeginForm("FilteredFlights", new { }, new AjaxOptions()
    {
        HttpMethod = "Post",
        UpdateTargetId = "flightPanel",
        InsertionMode = InsertionMode.Replace,
    }, new { id = "ajaxForm" }))
    {
        <input type="text" name="price" />
        <button type="submit" class="btn btn-warning">Search by Price</button>
    }
</div>
<div id="flightPanel" class="panel">
    @Html.Action("AllFlights")
</div>

@section scripts {
    <script>
        $(function () {
            $('[name="price"]').click()(function () {
                $('#ajaxForm').submit()
            })
        })
    </script>
}